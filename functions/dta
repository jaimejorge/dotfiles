# Usage: dta <url>
# Description: rip images from a 4chan board
dta() {
	wget -nc --no-check-certificate -O - $1 |
	grep -Eo 'i.4cdn.org/[^"]+' |
	uniq |
	xargs wget
	# remove dupes and thumbnails
	mv *.1 ~/.Trash
  mv *s.jpg ~/.Trash
}
