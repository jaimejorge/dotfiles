alias d='docker'
alias dps='docker ps'
alias dpsa='docker ps -a'
alias drm='docker rm -v'
alias di='docker images'

# Docker Machine
alias dm='docker-machine'
alias ddev='eval "$(docker-machine env dev)"'

# Docker Compose
alias dco='docker-compose'
alias dcop='docker-compose -p'

# Docker Swarm
alias ds='docker-swarm'
